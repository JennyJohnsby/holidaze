export default function venueCreateView() {
  const container = document.getElementById("venueContainer");
  if (container) {
    container.innerHTML = "<h2>Create Venue Form will go here</h2>";
  }
}
venueCreateView();
