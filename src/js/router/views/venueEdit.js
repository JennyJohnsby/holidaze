export default function venueEditView() {
  const container = document.getElementById("venueContainer");
  if (container) {
    container.innerHTML = "<h2>Edit Venue Form will go here</h2>";
  }
}
venueEditView();
